# 实施进展（B + C 并行）

> 更新日期：2026-09-17

## 已完成（本机可做部分全部完成）

### C — 宿主分发层（最小闭环的骨架）

| 文件 | 内容 | 状态 |
|---|---|---|
| `android/operit_quickjs/` | Operit QuickJS 引擎移植 | ✅ 包名已改写为 `com.psyche.kelivo.quickjs`，零残留 |
| `android/operit_quickjs/src/main/cpp/operit_git_source.cmake` | 补齐 CMake 辅助脚本（原缺失，构建必需） | ✅ |
| `android/app/src/main/kotlin/com/psyche/kelivo/quickjs/OperitHostDispatcher.kt` | **7 个宿主方法的分发表** | ✅ 已写 |
| `.../KelivoHost.kt` | 宿主能力接口（契约即实测结果） | ✅ 已写 |
| `.../QuickJsHostEnvironment.kt` | runtime 装配（对接 `QuickJsNativeHostDispatcher`） | ✅ 已写 |

已实现的 dispatch 分支（`super_admin` 全覆盖）：

```
Tools.System.terminal.create
Tools.System.terminal.exec
Tools.System.terminal.screen
Tools.System.terminal.input
Tools.System.shell
Tools.Files.mkdir
Tools.Files.write
```

> `terminal_wait` 不需要单独分支——它在 JS 里用 marker 命令基于 `terminal.exec` 实现（已实测确认）。

### Gradle 接入

| 改动 | 文件 |
|---|---|
| 注册模块 | `android/settings.gradle.kts`（+`com.android.library` 插件、+`include(":operit_quickjs")`） |
| 模块配置 | `android/operit_quickjs/build.gradle.kts`（去 version-catalog、Java 11、三 ABI、钉 NDK 版本） |
| app 依赖 | `android/app/build.gradle.kts`（`implementation(project(":operit_quickjs"))`） |
| 资源打包 | `pubspec.yaml`（`- assets/operit_packages/`） |

### B — CI 编译链路

`.github/workflows/build-merged-apk.yml`：
- Flutter **3.44.9**（对齐 pubspec 要求）/ Java 17 / zulu
- 手动触发可选 ABI，push 自动构建
- **自动生成临时 keystore** → 即使不配 secrets 也能产出**可安装**的 release APK
- 产出物上传为 artifact

## 尚未完成（阻塞项，必须换环境或有前置依赖）

| 项 | 阻塞原因 |
|---|---|
| `KelivoHost` 的真实实现 | 需对接 `workspace/PtySession.kt`、`ExecRunner.kt` 的完整 API（本轮只取了签名），且**无法本地编译验证** |
| 同步语义适配 | Kelivo 的 `exec` 是异步的（输出走 EventChannel），而 `super_admin` 要求同步返回 `{output,exitCode,timedOut}` — 需要在 Kotlin 侧做缓冲/等待 |
| Dart 侧包加载器 | 解析 METADATA → 生成工具 schema → 注册进 tool loop |
| 真机验证 | 无 Android 设备 |
| `flutter analyze` / 编译 | 无 Flutter SDK，aarch64 无 Android build-tools |

## 下一步（按优先级）

1. **push 到 GitHub 触发一次 CI**，确认 Gradle 接入与 CMake 拉取 QuickJS 能走通（这一步能立刻暴露配置错误）。
2. 实现 `WorkspaceKelivoHost`：用 `ExecRunner` + `ProotCommand` 支撑 `terminalExec`，用 `PtySessions` 支撑终端语义。
3. 写 Dart 侧包加载器（`assets/operit_packages/*.js` → 工具 schema）。
4. 真机跑 `super_admin:terminal` 的 `ls -la` 最小闭环。

## 诚实声明

本次改动的 Kotlin 代码**未经过编译**（本机无 Flutter/Android SDK）。
已做的是：包名一致性核查、接口签名逐一比对上游源码、契约用 Node 实测验证。
编译错误风险集中在 `KelivoHost` 的实现阶段，而非当前骨架。
